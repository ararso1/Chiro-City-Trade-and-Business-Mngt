# Server
PORT=3003

# Database - PostgreSQL (database name: trader_db)
# Replace 'postgres' with your actual postgres user PASSWORD. If you never set one, try leaving it empty:
#   DATABASE_URL="postgresql://postgres@localhost:5432/trader_db?schema=public"
# Or set the password you chose when installing PostgreSQL.
# Create the DB first in psql: CREATE DATABASE trader_db;
DATABASE_URL="postgresql://postgres:1234@localhost:5432/trader_db?schema=public"

# JWT
JWT_SECRET=chiro-city-trade-secret-change-in-production
JWT_EXPIRES=7d

# Optional: MESOB integration base URL
# MESOB_BASE_URL=https://mesob.ciroocity.com
